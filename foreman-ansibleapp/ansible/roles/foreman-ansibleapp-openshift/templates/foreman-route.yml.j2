apiVersion: v1
kind: Route
metadata:
  labels:
    app: foreman-ansibleapp
    service: foreman
  name: foreman
spec:
  port:
    targetPort: port-443
  tls:
    caCertificate: |
{{ ca }}
    certificate: |
{{ crt }}
    insecureEdgeTerminationPolicy: Allow
    key: |+
{{ key }}
    termination: edge
  to:
    kind: Service
    name: foreman
    weight: 100
